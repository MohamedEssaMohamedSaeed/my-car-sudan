name: my_car_sudan
description: نظام إيجاد المركبات المفقودة في السودان
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
  
  
  dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  flutter_localizations:  # أضف هذا
    sdk: flutter
  intl: any  # وأضف هذا